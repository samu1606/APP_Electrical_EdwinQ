# Backend Environment Variables Template
# Copia este archivo como .env y ajusta los valores

# URL de conexión a la base de datos
# Formato: postgresql+psycopg://usuario:password@host:puerto/nombre_db
DATABASE_URL=postgresql+psycopg://usuario:password123@postgres:5432/mi_app_db

# Configuración de Postgres (si usas el contenedor del compose)
POSTGRES_USER=usuario
POSTGRES_PASSWORD=password123
POSTGRES_DB=mi_app_db
